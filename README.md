# Secure-Password-Passphrase-Generator
A Python cybersecurity tool that generates secure passwords and passphrases using cryptographically strong randomness. It calculates password entropy, evaluates security strength, prevents duplicates, and allows users to export generated credentials to CSV or JSON files.
